import React from "react";

function Add(){
    let add=0;
    return (
        <div>
            <input type={number} />
            <input type={number} />
        </div>
    );
}

export default Add()